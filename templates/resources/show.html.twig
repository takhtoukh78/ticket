{% extends 'base.html.twig' %}

{% block title %}Resources{% endblock %}

{% block body %}
    <h1>Resources</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ resource.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ resource.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ resource.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ resource.image }}</td>
            </tr>
            <tr>
                <th>Video</th>
                <td>{{ resource.video }}</td>
            </tr>
            <tr>
                <th>Pdf</th>
                <td>{{ resource.pdf }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_resources_index') }}">back to list</a>

    <a href="{{ path('app_resources_edit', {'id': resource.id}) }}">edit</a>

    {{ include('resources/_delete_form.html.twig') }}
{% endblock %}
