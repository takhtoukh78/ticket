<form method="post" action="{{ path('app_ticket_conversation_delete', {'id': ticket_conversation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ticket_conversation.id) }}">
    <button class="btn">Delete</button>
</form>
