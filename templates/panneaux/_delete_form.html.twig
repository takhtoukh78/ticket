<form method="post" action="{{ path('app_panneaux_delete', {'id': panneaux.idPa}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ panneaux.idPa) }}">
    <button class="btn">Delete</button>
</form>
